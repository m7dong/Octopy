from PartialModel import Partial_Model 


class GPU_Container:
    def __init__(self, partial_model, users=[]):
    	self.partial_model = partial_model
    	self.users = users

    def add_user(user):
    	self.users.append(user)